
import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((themeMain) => ({

  appBar: {
    height: "70px",
    justifyContent: "center",
    boxShadow:"none",
    marginBottom:"40px",
  },
  buttonMenu:{
    fontSize:"10px"
  },
}));

export default useStyles;
